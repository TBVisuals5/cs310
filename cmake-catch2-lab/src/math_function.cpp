#include "math_functions.h"
#include <cmath>
using namespace std;

double compute_square_root(double number){
    if (number < 0) {
        return -1; //Error for negative numbers
    }
    return (number);
}